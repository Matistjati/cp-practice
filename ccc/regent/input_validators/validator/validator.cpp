#include "validator.h"
#include <set>

void run() {
    int n = Int(0,40); Space(); int m = Int(0,40); Endl();
    SpacedInts(20, 0, 1000);
    for (int i = 0; i < n; i++) {SpacedInts(21, 0, 1000); }
    Eof();
}
